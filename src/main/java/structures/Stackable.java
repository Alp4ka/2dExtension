package structures;

/**
 * Interface describes objects which can make 'stacks' wth other
 * objects(Origins, Points).
 *
 * @author Gorkovets Roman
 * @version 1.0
 */
public interface Stackable {
    BoundBox getBounds();
}
